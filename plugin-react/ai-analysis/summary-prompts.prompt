請執行以下操作：

1. 讀取 plugin-react/ai-analysis/summary-prompts.yaml 檔案
2. 遍歷 YAML 中的每個第一層 key（每個 key 應包含 output_path 和 prompt 欄位）
3. 對於每個項目：
   - 讀取其 prompt 欄位內容
   - 分析 plugin-react/ai-analysis 目錄下所有股票分析檔案
   - 依據 prompt 中的評估標準進行篩選
   - 將結果輸出至該項目的 output_path 欄位指定的路徑

完成後請回報各分析的執行結果摘要。